schema_version: "1.1"
name: "domain-modeling--04-negativo-estilo-go"
description: "Roda o gofmt e ajusta a ordem dos imports deste arquivo Go para o padrão do goimports: package main; import (\"os\"; \"fmt\"); func main() { fmt.Println(os.Args) }"
tags:
  - "negativo"
execution:
  max_turns: 10
  allowed_tools:
    - "Read"
    - "Glob"
    - "Grep"
    - "Skill"
