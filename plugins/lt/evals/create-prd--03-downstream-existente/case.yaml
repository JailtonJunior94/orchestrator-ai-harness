schema_version: "1.1"
name: "create-prd--03-downstream-existente"
description: "Atualiza o PRD do extrato CSV para incluir exportação em PDF. Já existe techspec e tasks aprovadas para esse PRD."
tags:
  - "roteamento"
execution:
  max_turns: 8
  allowed_tools:
    - "Read"
    - "Glob"
    - "Grep"
    - "Skill"
